import React from 'react'

const Country = () => {
    return (
        <div>
            <select disabled>
                <option defaultValue="Polska">Polska</option>
            </select>
        </div>
    )
}

export default Country
